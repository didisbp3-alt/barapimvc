@model MVCAPIFriedBananas.Views.ViewModels.MenuItemViewModel
@{
    ViewData["Title"] = "Novo Menu";
}

<div class="d-flex justify-content-center align-items-center" style="min-height: 80vh;">
    <div class="card" style="max-width: 480px; width: 100%; padding: 2rem;">
        <h2 class="mb-3 text-center" style="font-weight:700; color:var(--brand);">Novo Menu</h2>
        @if (!ViewData.ModelState.IsValid)
        {
            <div class="alert alert-danger">
                <ul>
                    @foreach (var error in ViewData.ModelState.Values.SelectMany(v => v.Errors))
                    {
                        <li>@error.ErrorMessage</li>
                    }
                </ul>
            </div>
        }
        <form asp-action="Create" method="post" autocomplete="off">
            <div class="form-group mb-3">
                <label asp-for="Date" class="form-label"></label>
                <input asp-for="Date" class="form-control" type="date" />
            </div>
            <div class="form-group mb-3">
                <label asp-for="MainDish" class="form-label"></label>
                <input asp-for="MainDish" class="form-control" />
            </div>
            <div class="form-group mb-3">
                <label asp-for="Soup" class="form-label"></label>
                <input asp-for="Soup" class="form-control" />
            </div>
            <div class="form-group mb-3">
                <label asp-for="Dessert" class="form-label"></label>
                <input asp-for="Dessert" class="form-control" />
            </div>
            <div class="form-group mb-3">
                <label asp-for="MaxSeats" class="form-label"></label>
                <input asp-for="MaxSeats" class="form-control" type="number" />
            </div>
            <button type="submit" class="btn btn-primary w-100 btn-small mb-2" style="font-weight:700;">Guardar</button>
            <div class="text-center mt-2">
                <a asp-action="AdminIndex" class="btn btn-ghost btn-small">Cancelar</a>
            </div>
        </form>
    </div>
</div>

